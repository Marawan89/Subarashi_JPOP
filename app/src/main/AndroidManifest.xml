<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- application -->
    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SubarashiJPOP"
        tools:targetApi="31">

        <!-- anime-related activities -->
        <activity
            android:name=".activities.anime.AnimeListActivity"
            android:exported="false" />

        <activity
            android:name=".activities.anime.AnimeFavListActivity"
            android:exported="false" />

        <!-- manga-related activities -->
        <activity
            android:name=".activities.manga.MangaListActivity"
            android:exported="false" />

        <activity
            android:name=".activities.manga.MangaFavListActivity"
            android:exported="false" />

        <!-- main activity -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>